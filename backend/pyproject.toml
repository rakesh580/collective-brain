[project]
name = "collective-brain"
version = "0.1.0"
description = "AI-powered shared memory and strategy brain for small groups"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
